<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.WaterIntake" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Force window background to black -->
        <item name="android:windowBackground">#000000</item> <!-- ✅ Black background -->
        <!-- You can also customize your light theme colors here if needed -->
    </style>

    <!-- App's main theme -->
    <style name="Theme.WaterIntake" parent="Base.Theme.WaterIntake" />

    <!-- 🔥 NEW: Launcher theme for app startup -->
    <style name="Theme.WaterIntake.Launcher" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">#000000</item> <!-- ✅ Black splash screen -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
